<main>
    <section id="catalog">
        <h1>All posts</h1>
        <div class="band">
            {{#each posts}}
            {{>post}}
            {{else}}
            <p class="no-offer">There are no posts yet...</p>
            {{/each}}
        </div>
        </div>
    </section>
</main>