<main>
    <section id="my-posts">
        <h1>My post</h1>

        <div class="my-container">
            {{#each posts}}
            {{>profilePost}}
            {{else}}
            <div class="no-posts">
                <p class="no-offer">There are no own posts yet...</p>
            </div>
            {{/each}}
        </div>
        </div>
    </section>

</main>