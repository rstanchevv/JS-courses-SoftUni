function twoKeyPlus(input){
    let number = Number(input[0]);
    let index = 1;
    let nextNumber = 1;
    while (nextNumber <= number){
        console.log(nextNumber)
        nextNumber = (nextNumber) * 2 + 1;
        index
    }
}
twoKeyPlus(["8"])